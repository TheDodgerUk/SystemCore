using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class VisualEffectLocal : GameObjectLocal
{
    protected override string ASSET_TYPE => nameof(VisualEffectLocal);
    public override string FileExtention => ".prefab";
}